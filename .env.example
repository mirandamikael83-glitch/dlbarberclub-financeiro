# Estas variáveis são criadas AUTOMATICAMENTE pela Vercel quando você
# conecta um banco Postgres ao projeto (aba "Storage" no dashboard).
# Não é necessário preencher isso manualmente em produção.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
